[workspace]
members = ["marty-python-adapters", "marty-types", "marty-crypto", "marty-crypto-test-support", "marty-verification", "marty-secure-storage", "marty-biometrics", "marty-iso18013", "marty-zkp", "marty-status", "marty-bindings", "marty-oid4vci", "marty-oid4vci/tests/ui/wasm-serial-probe", "marty-didcomm", "marty-test-wallet"]
resolver = "2"

[workspace.package]
version = "0.2.0"
edition = "2021"
authors = ["Marty Team"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/ElevenID/marty-core"
rust-version = "1.95"

[workspace.dependencies]
# Crypto and certificates
x509-cert = { version = "0.2", default-features = false, features = ["pem", "std"] }
der = { version = "0.7", features = ["pem", "derive"] }
const-oid = "0.9"
sha1 = { version = "0.10", features = ["oid"] }
sha2 = { version = "0.10", features = ["compress"] }
p256 = { version = "0.13", default-features = false }
p384 = { version = "0.13", default-features = false }
p521 = { version = "0.13", default-features = false }
ecdsa = "0.16"
elliptic-curve = "0.13"
pem-rfc7468 = "0.7"
signature = "2.0"

# RSA support
rsa = { version = "0.9", default-features = false, features = ["std", "sha2", "u64_digit"] }

# PKCS standards
pkcs8 = { version = "0.10", default-features = false, features = ["pem", "std"] }
spki = { version = "0.7", features = ["pem"] }

# CMS/CRL/OCSP (PKCS#7, X.509 extensions)
cms = { version = "0.2", features = ["std"] }
x509-ocsp = "0.2"

# Symmetric encryption
aes-gcm = { version = "0.10", default-features = false, features = ["aes", "alloc", "zeroize"] }
aws-lc-rs = { version = "1.18", default-features = false, features = ["aws-lc-sys", "legacy-des"] }
aes = { version = "0.8", features = ["zeroize"] }
ghash = { version = "0.5", features = ["zeroize"] }
polyval = { version = "0.6", features = ["zeroize"] }
cbc = { version = "0.1", features = ["alloc"] }
des = { version = "0.8", features = ["zeroize"] }

# Key derivation
hkdf = "0.12"
pbkdf2 = { version = "0.12", features = ["hmac"] }

# Additional crypto
hmac = "0.12"
cmac = { version = "0.7", features = ["zeroize"] }
cipher = { version = "0.4", features = ["block-padding"] }

# Ed25519 and X25519
ed25519-dalek = { version = "2.1", default-features = false, features = ["std", "zeroize"] }
x25519-dalek = { version = "2.0", features = ["static_secrets"] }

# Ed448 (for eMRTD Active Authentication)
# Marty implements the public verification equation over the backend's public
# curve/scalar types. Do not select its combined signing/verification module.
ed448-goldilocks-plus = { version = "0.16", default-features = false }

# BBS+ signatures (BLS12-381)
zkryptium = { version = "0.6", default-features = false, features = ["bbsplus"] }

# PKCS#12 (for key/cert bundle parsing)
p12 = "0.6"

# Random number generation
rand = "0.8"
rand_core = "0.10"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ciborium = "0.2"

# Async runtime
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.13", default-features = false, features = ["form", "json", "rustls"] }

# Caching
moka = { version = "0.12", features = ["future"] }

# Error handling
thiserror = "2.0"
anyhow = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tracing-error = "0.2"
axum = "0.8"

# Database (for marty-secure-storage)
rusqlite = { version = "0.39", features = ["bundled-sqlcipher-vendored-openssl"] }
keyring = "4.1"
keyring-core = "1.0"
directories = "6.0"
blake3 = "1.5"

# UUID
uuid = { version = "1.0", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
base64 = "0.22"
zeroize = "1.7"

# Python bindings
pyo3 = { version = "0.29.0", features = ["abi3-py311"] }

# Internal crates
marty-crypto = { version = "0.2.0", path = "marty-crypto" }
marty-verification = { version = "0.2.0", path = "marty-verification" }
marty-secure-storage = { version = "0.2.0", path = "marty-secure-storage" }
marty-biometrics = { version = "0.2.0", path = "marty-biometrics" }
marty-status = { version = "0.2.0", path = "marty-status" }

# Test/dev dependencies
rcgen = "0.14"
tempfile = "3.14"
proptest = "1.5"
criterion = "0.8.2"

[profile.release]
lto = true
opt-level = 3
codegen-units = 1

[profile.dev]
opt-level = 0
debug = true

[patch.crates-io]
# Temporary security fork pending https://github.com/spruceid/ssi/pull/706.
ssi-jwt = { git = "https://github.com/ElevenID/ssi-jwt-elevenid", rev = "957c8b0fbf3e4b967fe73c858a22b38e907b3023" }
# Exact history is retained on a dedicated dependency ref in the existing SSI fork.
linked-data = { git = "https://github.com/ElevenID/ssi-elevenid", rev = "6f86efc1579033e14ff2ad7d115ca0857e16a67f" }
# ElevenID-maintained security/interoperability fork, synchronized from upstream by reviewed monthly PRs.
isomdl = { git = "https://github.com/ElevenID/isomdl-elevenid", rev = "784a52943469622873c7ae3200dbc11e89d6bd8e" }
