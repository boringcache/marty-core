[package]
name = "marty-didcomm"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "DIDComm Messaging 2.1 credential-delivery support for Marty"
keywords = ["didcomm", "did", "verifiable-credentials", "messaging"]
categories = ["authentication", "cryptography"]

[lib]
name = "marty_didcomm"
crate-type = ["rlib"]

[features]
default = ["did_key", "did_web", "did_peer", "did_jwk"]
kms-only = []
# Software anoncrypt creates a short-lived X25519 key and currently delegates
# to a dependency that also compiles unrelated signing backends. Keep that
# dependency out of resolver-only and KMS-only base artifacts.
encrypted-envelope = ["dep:affinidi-messaging-didcomm"]
local-key-operations = ["encrypted-envelope"] # Caller-supplied long-lived DIDComm sender/recipient private keys
did_key = []
did_web = ["dep:reqwest", "dep:tokio"]
did_peer = []
did_jwk = []

[dependencies]
# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Crypto — DID resolution and standards-conformant DIDComm envelopes
ed25519-dalek = { workspace = true }
p256 = { workspace = true, features = ["arithmetic"] }
base64 = { workspace = true }
affinidi-messaging-didcomm = { version = "=0.15.8", default-features = false, optional = true }

# DID document parsing + resolution
uuid = { workspace = true }
chrono = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
url = "2.5"
bs58 = "0.5"
urlencoding = "2.1"

# HTTP client for did:web resolution (optional)
reqwest = { version = "0.13", default-features = false, features = ["rustls", "json"], optional = true }
tokio = { workspace = true, optional = true }

[dev-dependencies]
tokio = { workspace = true }
